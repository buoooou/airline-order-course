server.port=8080


# spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:testdb}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# spring.datasource.username=${DB_USER:root}
# spring.datasource.password=${DB_PASS:my-secret-pw}

# 数据源配置
spring.datasource.url=jdbc:postgresql://localhost:5432/airline_order_db?useUnicode=true&characterEncoding=utf8
spring.datasource.username=postgres
spring.datasource.password=root
spring.datasource.driver-class-name=org.postgresql.Driver

# ----------------- MODIFY JPA/HIBERNATE CONFIG -----------------
# ddl-auto=update
spring.jpa.show-sql=true
# 当应用正常关闭时，Hibernate 会自动删除所有创建的表（清空数据库）。
# spring.jpa.hibernate.ddl-auto=create-drop
# 只更新表结构，不删除数据（推荐开发使用）。
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
# SpringDoc (Swagger)
springdoc.swagger-ui.path=/swagger-ui.html

jwt.secret=63ffbc2b8d13ad5180ed7ae7c67f18c85d86046732fc9ced6a02a9d50abb1a03
jwt.expiration.ms=86400000

# Redis Configuration(Redis 配置优化)
spring.data.redis.host=localhost
spring.data.redis.port=6379
# 如果有密码则取消注释并填写
# 若启用密码，建议通过环境变量注入而非明文 
# 正确示例：
# spring.data.redis.password=${REDIS_PASSWORD}
spring.data.redis.database=0
# 增加重试机制更可靠
spring.data.redis.timeout=3000ms
# 建议新增
spring.data.redis.lettuce.shutdown-timeout=100ms

# Redis Lettuce Pool Configuration
spring.data.redis.lettuce.pool.enabled=true
# 生产环境建议16-32（根据并发量调整）
spring.data.redis.lettuce.pool.max-active=8
# 应与max-active一致避免频繁扩容
spring.data.redis.lettuce.pool.max-idle=8
# 建议设为2保持最小热连接
spring.data.redis.lettuce.pool.min-idle=0
spring.data.redis.lettuce.pool.max-wait=-1ms
# 连接泄漏检测
spring.data.redis.lettuce.metrics.enabled=true
spring.data.redis.lettuce.tracing.enabled=true

# Email Configuration
spring.mail.host=smtp.example.com
spring.mail.port=587
spring.mail.username=your-email@example.com
spring.mail.password=your-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

logging.file.name=logs/application.log
logging.level.root=INFO

spring.jpa.open-in-view=false