# Airline Order Backend (Simplified)

## How to run
```bash
mvn clean spring-boot:run
# Then open: http://localhost:8080/swagger-ui.html
# Login with: POST /api/auth/login  body: {"username":"admin","password":"admin123"}
# Use token in Authorize (Bearer)
```
