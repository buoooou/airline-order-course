server.port=8080

# 数据库配置 - 支持远程和本地数据库切换
# 远程数据库配置（优先使用）
spring.datasource.primary.url=jdbc:mysql://18.207.201.92:3306/airline_order_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&connectTimeout=5000&socketTimeout=10000
spring.datasource.primary.username=airline
spring.datasource.primary.password=airlineTest1234

# 本地数据库配置（备用）
spring.datasource.fallback.url=jdbc:mysql://localhost:3306/airline_order_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true&zeroDateTimeBehavior=convertToNull&allowMultiQueries=true
spring.datasource.fallback.username=root
spring.datasource.fallback.password=rootroot

# 当前使用的数据库配置（通过环境变量或配置文件控制）
spring.datasource.url=${spring.datasource.primary.url}
spring.datasource.username=${spring.datasource.primary.username}
spring.datasource.password=${spring.datasource.primary.password}

# 数据库连接池配置
spring.datasource.hikari.connection-timeout=10000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

# JPA/Hibernate配置
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# SpringDoc (Swagger)配置
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs

# JWT配置
jwt.secret=63ffbc2b8d13ad5180ed7ae7c67f18c85d86046732fc9ced6a02a9d50abb1a03
jwt.expiration.ms=86400000

# 日志配置
logging.level.com.postion.airlineorderbackend=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# =================================================================
# 定时任务配置
# =================================================================
# 订单超时配置
app.order.payment-timeout-minutes=30
app.order.ticketing-timeout-minutes=60
app.order.ticketing-failed-timeout-hours=24

# 定时任务开关（可用于临时禁用定时任务）
app.scheduled.enabled=true

# ShedLock配置
app.shedlock.default-lock-at-most-for=10m
app.shedlock.table-name=shedlock
